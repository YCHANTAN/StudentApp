export type ScheduleEntry = {
  id: string;
  studentId: string;
  dayLabel: string;
  courseCode: string;
  courseTitle: string;
  timeRange: string;
  room: string;
  instructor: string;
  createdAt: Date;
};
